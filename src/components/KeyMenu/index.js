import KeyMenu from './KeyMenu';


const returnKeyMenu = (config) => {
  // let { content, duration, type } = config;
  return new KeyMenu({
    // content,
    // duration,
    // type
  });
}

export {
  returnKeyMenu
};
